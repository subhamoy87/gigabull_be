# Workspace Rules

- **Do NOT push directly to GitHub/remote repository (especially `main` branch)**. Only perform local changes/commits if asked. The user will manually handle all `git push` operations through Source Control.
